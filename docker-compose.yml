services:
  # 解説動画の台本〜字幕生成サービス
  narrative-script:
    build:
      context: .
      dockerfile: services/narrative-script/Dockerfile
    volumes:
      - .:/workspace
    working_dir: /workspace/services/narrative-script
    env_file:
      - .env
    stdin_open: true
    tty: true
